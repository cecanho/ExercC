#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	puts("Texto com a configuracao do prompt.");
	system("Color 1E");
	puts("Texto com a configuracao do prompt.");
	return 0;
}
